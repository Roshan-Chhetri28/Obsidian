## 25. Testing for thrown error

- we sometimes expect the unit to throw error of some kind we can write the tests for this as well
	- we use .toThrow();
	- and we can pass regex like ``` .tothrow(/ is not iterable ```
	- 
	- the .toThrow() can receive
		- string
		- constructible (we can check if the error belongs to that class or not)
		- Regex Expression
		- Error

## 26 and 27 is just exercise:
- Learned that we can use .toBeTypeof() function to check if the value we returned is of some type.