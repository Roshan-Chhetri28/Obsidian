After writing the test we should run test  (IG which is pretty obvious since we have written it)
- To run test use the test script which we have added in package.json i.e npm test
-  --reporter --verbose: gives us detailed output about the test 