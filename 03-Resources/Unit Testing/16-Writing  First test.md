Summary of what I understood
1.it ![[15-Unit Testing (lecture 15)]] (the test function) takes two params 
	1. a string summarizing what that unit should do 
	2. a function evaluating the test
		1. call the unit here (import that unit in the test file this is pretty obvious ) 
			1. we can use expect (should be imported) which expect result returned form the unit to full-fill some condition.