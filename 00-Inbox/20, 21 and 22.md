## Keep test Simple (20):
1. the test should be ==as simple as possible== 
	- for example: if the function is performing sum of numbers then we can write 2-3 number and get it done with
2. unit as possible 
3. test should be written in unit code like unit function (meaning the function which does not call any other function)
<hr>
## Define Behaviors & Fixing Errors in your code
-  Always remember we can & ==should write multiple test for a unit== to cover all/most of the known cases;
	- for example: function to return sum of numbers.
	- check if the returned value is of type number.
	- check if the retuned value is the correct sum.
	- check it covers summing array of string. 
	- check if the function returns NaN on sum of Invalid type of data.
## Writing More test
- writing good test is am iterative process
- 